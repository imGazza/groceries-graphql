meta {
  name: 03 - Refresh Token
  type: graphql
  seq: 3
}

post {
  url: https://localhost:7298/graphql
  body: graphql
  auth: inherit
}

headers {
  Cookie: refreshToken=IgRI9PO/juF6qHSWMb03pUgcO1DRnRSIwn0z9Pvzygs=
}

body:graphql {
  mutation RefreshToken(){
    refreshToken(){
      accessToken,
      user{
        id,
        email,
        firstName,
        lastName,
        roles
      }
    }
  }
}

script:post-response {
  var jsonData = res.getBody();
  bru.setEnvVar("access-token", jsonData.data.refreshToken.accessToken);
}
